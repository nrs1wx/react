# About Me (React SPA)

Personal one-page website built with React and Vite.

- `npm install` - install packages
- `npm run dev` - run locally
- `npm run deploy` - build and publish to GitHub Pages (branch `gh-pages`)

Edit your information in `src/data.js`. Replace `src/assets/avatar.svg` with your photo.
